<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cy.mvpframetest">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_play_launch"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_play_launch"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.MainActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".content.ContentActivity"/>
        <activity android:name=".knowledge.knowledgeList.KnowledgeListActivity"/>
        <activity android:name=".login.LoginInActivity"/>
        <activity android:name=".login.SignUpActivity"/>
    </application>

</manifest>
